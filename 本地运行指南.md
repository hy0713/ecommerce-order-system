# 本地运行指南

> 结论：**四套系统（单体 / 微服务 / 前端 / AI 客服）全部可在本机跑通并完成端到端演示。**
> 本文件记录前置条件、一键启动方式、实测验证结果与已知环境坑。

---

## 一、环境要求

| 组件 | 版本要求 | 本机实测 |
|---|---|---|
| JDK | 17 | 17.0.12 ✅ |
| Node.js | 18+ | 22.22.2 ✅ |
| Python | **必须 3.12**（3.14 装不上 fastembed 0.2.6） | 3.12.10 ✅ |
| Docker Desktop | 任意较新版本 | 29.4.1 ✅ |
| Maven | 无需单独安装（跟随 wrapper，或见下方说明） | 3.9.9（已缓存） |

浏览器：演示用 Chrome / Edge 均可。

---

## 二、一键启动（推荐）

```bash
cd E:/cscode/java/shop
bash start-all.sh
```

脚本按固定顺序启动并逐步做健康检查，失败会立即报错而不是静默跳过：

```
0. 前置检查（java / node / docker / Agent 虚拟环境）
1. 中间件：MySQL 3306 · Redis 6379 · RabbitMQ 5672 · Redis Stack 6380
2. 数据库增量迁移（user.role / stock_compensation，幂等可重复执行）
3. Nacos 注册中心 8848
4. 微服务：shop-user 8081 · shop-product 8082 · shop-order 8083 · shop-gateway 8080
5. AI 智能客服 8000
6. Vue 管理后台 5173
```

停止：

```bash
bash stop-all.sh                # 只停应用，保留中间件容器
bash stop-all.sh --with-docker  # 连中间件一起停
```

首次使用若未构建过 jar：

```bash
cd shop-parent && mvn clean package -DskipTests && cd ..
```

> **Nacos 路径**：脚本默认 `NACOS_HOME=/e/cscode/java/tools/nacos-2.2.3`。
> 若你的 Nacos 在别处：`NACOS_HOME=/your/path bash start-all.sh`

---

## 三、访问地址与演示账号

| 入口 | 地址 | 说明 |
|---|---|---|
| 管理后台 | http://localhost:5173 | **必须用 `localhost`**，本机 `localhost` 解析到 `::1`，写 `127.0.0.1` 会连不上 |
| 网关 API | http://localhost:8080 | 所有接口统一入口 |
| Nacos 控制台 | http://localhost:8848/nacos | nacos / nacos |
| RabbitMQ 控制台 | http://localhost:15672 | guest / guest |
| AI 客服文档 | http://localhost:8000/docs | Swagger |
| 各服务接口文档 | http://localhost:8081/doc.html 等 | 8081 / 8082 / 8083 |

**演示账号：`admin` / `123456`**（角色 ADMIN）
> 自助注册的账号是普通用户 USER，**没有发货 / 完成订单 / 查看经营统计的权限**（会返回 403）。
> 演示管理动作请务必用 admin 登录。

### 小程序

```bash
cd ecommerce-mini-program && node node_modules/@dcloudio/vite-plugin-uni/bin/uni.js build -p mp-weixin
```
微信开发者工具「导入项目」选择 `ecommerce-mini-program/dist/build/mp-weixin`。
工程已内置 `urlCheck: false`，可直接访问 `http://localhost:8080`，无需配置合法域名。

---

## 四、三条推荐演示路径

**① 管理后台完整业务闭环（最有说服力）**
登录（admin/123456）→ 数据概览 → 商品管理 → 订单管理 → 点「模拟下单（闭环演示）」
→ 选商品 → 下单 → 订单出现在列表 → 回到数据概览看统计实时变化。

**② AI 智能客服**
管理后台「智能客服」页 → 新建会话 → 提问（如「七天内无理由退货的运费怎么算？」）
→ 上传售后政策文档后再次提问，可观察到 RAG 检索命中知识库的回答。

**③ 接口文档**
http://localhost:8081/doc.html 等，Knife4j 上直接调试。

---

## 五、验收脚本（实测全部通过）

| 脚本 | 覆盖 | 实测结果 |
|---|---|---|
| `bash scripts/smoke-test.sh` | 阶段一单体全链路 | **54/54 PASS** |
| `bash scripts/concurrency-test.sh` | 阶段一并发无超卖 | **PASS**（20 并发×库存5 → 5 单、库存 0） |
| `bash shop-parent/scripts/smoke-test-ms.sh` | 阶段二微服务全链路（经网关） | **40/40 PASS** |
| `bash shop-parent/scripts/concurrency-test-ms.sh` | 阶段二并发无超卖 | **PASS** |
| `cd ecommerce-agent-python && bash scripts/smoke_test.sh` | AI 客服全链路 | **13/13 PASS** |
| `cd shop-web && node e2e-check.js` | **真实浏览器端到端**（登录→各页→下单闭环） | **12/12 PASS**，全流程零失败请求 |

> `shop-web/e2e-check.js` 用 CDP 驱动本机 Chrome，不需要额外安装任何依赖，
> 会自动截图到 `shop-web/e2e-shots/`，适合改动后跑一遍回归。

**阶段一与阶段二不能同时启动**——单体应用和网关都监听 8080。切换方式：

```bash
bash stop-all.sh
cd shop-parent && mvn clean package -DskipTests   # 如需
# 注意：JWT 密钥没有配置默认值（缺失即启动失败），必须显式提供
cd .. && JWT_SECRET='local-dev-change-me-jwt-secret-please-override-in-production' \
  java -Duser.timezone=Asia/Shanghai -jar target/helpbydsv4.jar
```

---

## 六、本机已知环境坑（脚本已处理，手工操作时注意）

| 坑 | 现象 | 处理 |
|---|---|---|
| **系统代理** | `http_proxy=http://127.0.0.1:2859`，curl 访问 localhost 被拦成 **502** | 所有 curl 加 `--noproxy '*'`；Python 进程设 `no_proxy=*` |
| **curl 读不到 /tmp** | Windows 原生 curl 无法解析 MSYS 的 `/tmp/x`，报 `error encountered when reading a file` | 用 `cygpath -w /tmp` 转成 Windows 路径 |
| **java 不认 MSYS 路径** | `java -jar /e/cscode/...` 报 `Unable to access jarfile` | `-jar` 参数用 `cygpath -w` 转 Windows 路径 |
| **`docker compose` 不可用** | 本机只有 standalone 版 | 用 `docker-compose`（v5.1.3） |
| **日志是 GBK 编码** | `grep` 搜中文搜不到 | 先 `iconv -f GBK -t UTF-8 日志文件` |
| **后台进程不跨终端存活** | 关掉终端进程就没了 | 保持终端开着，或用 `nohup`（脚本已用） |
| **`sc.exe` 被安全策略拉黑** | 无法查询 Windows 服务 | 用 `netstat` / `tasklist` 替代 |
| **DeepSeek 走代理偶发 502** | AI 客服返回兜底话术 | Agent 进程启动时设 `no_proxy=*` 直连（脚本已处理） |
| **RabbitMQ 队列参数变更** | 改 TTL 等参数后启动报 `PRECONDITION_FAILED` | `rabbitmqctl delete_queue <队列名>` 后重启 |
| **数据库结构变更** | 旧数据卷不会重跑 initdb | 执行 `sql/migration_2026_09.sql`（`start-all.sh` 已自动执行） |

---

## 七、故障排查顺序

1. **服务起不来** → 看 `logs/<服务名>.log`
2. **接口 401** → 检查是否带 token；白名单接口清单见 `AuthConstant.PUBLIC_ENDPOINTS`
3. **接口 403「无权限」** → 该操作需要 ADMIN 角色，用 admin 登录
4. **接口 403「内部接口不允许直接访问」** → 你在直连业务服务端口，应走网关 8080
5. **微服务之间调用失败** → 确认 `INTERNAL_TOKEN` 在网关与 4 个服务间一致（由 `start-all.sh` 统一注入，值相同）。
   若单独启动某个服务：该令牌**没有默认值**，不设置会直接启动失败，需自行 `export INTERNAL_TOKEN=...`
6. **AI 客服答非所问/兜底话术** → 先看 `/api/agent/health` 四项依赖，再查是否代理导致 LLM 调用失败
